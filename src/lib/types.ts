export interface Category {
  id: string
  slug: string
  label: string
}

export interface WordCount {
  word: string
  count: number
}

export interface HeadlineSummary {
  id: string
  title: string
  link: string
}
