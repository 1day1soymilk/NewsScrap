# 배치를 재는 자

`scripts/analysis/`가 체를 재듯이 이건 캔버스를 잰다. 배치를 바꾸는 작업은
"보기 좋아졌다"로 끝내기가 너무 쉽고, 이 저장소는 그 습관이 이미 여러 번 값을
치른 곳이다.

```bash
node --experimental-strip-types --import ./scripts/layout/register.mjs scripts/layout/measure.ts   # 지표 표
node --experimental-strip-types --import ./scripts/layout/register.mjs scripts/layout/bridges.ts   # xBr 분해
node scripts/layout/pullFixture.mjs scripts/layout/graphDays.json   # 픽스처 갱신
```

`register.mjs`가 필요한 것은 소스가 확장자 없는(번들러) import를 쓰기 때문이다 —
`resolveTs.mjs`가 `.ts`/`.tsx`를 붙여준다.

Node 24의 타입 스트리핑을 쓴다 — `tsx`도 `vite-node`도 의존성에 없고, 이 한
스크립트 때문에 하나 들이는 것은 과하다.

## 픽스처

`graphDays.json`은 배포된 프로젝트에서 뽑은 네 날(2026-07-31 ~ 08-03)의
`keyword_graph(day, null)` 응답이다. 노드 70개씩, 엣지 37 / 34 / 52 / 58.

**체가 바뀌면 픽스처부터 다시 떠야 한다.** 그려지는 70단어가 바뀌고, 엣지는
그려진 단어 사이에만 생기므로 엣지 집합도 같이 바뀐다 — 마이그레이션 0007이
기록한 함정("노드 집합을 고정한 점검은 그려진 그래프에 대한 점검이 아니다")이
여기에도 그대로 적용된다.

## 글자 폭은 브라우저와 다르다

Node에는 캔버스가 없으므로 `KeywordGraph.tsx`의 jsdom 대체식
(`length * fontSize * 0.95`)을 그대로 쓴다. 절대 픽셀 수는 화면과 다르다. 이 자는
**두 배치를 서로 비교하기 위한 것이지 화면을 예측하기 위한 것이 아니므로**
그것으로 충분하다 — 체 하네스가 설정끼리만 비교하고 남이 적어둔 퍼센트와는
비교하지 않는 것과 같은 규칙이다.

## 지표

| 열 | 뜻 |
| --- | --- |
| `drawn` | 실제로 그려진 엣지. `edges`보다 작으면 두 라벨이 너무 붙어 선 자리가 없었던 것 |
| `crowded` | `curve.clear === false` — 라벨을 못 피하고 글자 밑을 지나간 선 |
| `crossings` | 서로 교차하는 엣지 **쌍**. 끝점을 공유하는 쌍은 제외한다 — 그건 단어 위에서 만나는 것이지 눈이 길을 잃는 교차가 아니다 |
| `xIn` / `xBr` | 그 교차가 **한 구역 안**에서 난 것인지, 다리가 낀 것인지. 이 두 열을 나눠 보기 전까지 남은 난잡함이 다리 탓인 줄 알았고, 틀렸다 |
| `bridges` | 두 끝이 다른 구역에 있는 엣지. 없앨 것이 아니라 짧게 만들 것이다 |
| `lenMed`, `lenMax` | 엣지 길이(중심-중심) 중앙값과 최댓값 |
| `overlap` | 겹치는 라벨 박스 쌍. **0이어야 한다** |
| `height` | 크롭된 뷰포트 높이 |

## Baseline — 재작성 전 (커밋 4f80654 기준)

```
   view         day  nodes  edges  drawn  crowded  crossings  lenMed  lenMax  overlap  height
-------  ----------  -----  -----  -----  -------  ---------  ------  ------  -------  ------
desktop  2026-07-31     70     37     37        7          4      88     236        0  682.34
desktop  2026-08-01     70     34     34        2          0      80     302        0  818.91
desktop  2026-08-02     70     52     52       10         27      83     287        0  800.47
desktop  2026-08-03     70     58     58       10         19      82     208        0     723
  phone  2026-07-31     70     37     36       18          4      94     189        1  819.84
  phone  2026-08-01     70     34     34       21          6     100     332        0  914.72
  phone  2026-08-02     70     52     52       31         45     111     397        0  841.37
  phone  2026-08-03     70     58     56       30         13     110     359        0  816.76
```

읽을 것 세 가지:

- **난잡함은 하루의 크기를 따라간다.** 엣지가 제일 많은 08-02·08-03에서 교차가
  27과 19다. 08-01은 0이다 — 선이 34개뿐인 날은 지금 배치로도 읽힌다. 즉 고쳐야
  할 것은 "선이 많은 날"이고, 그날이 뉴스가 제일 많은 날이다.
- **폰에서는 이미 무너져 있다.** 358px에서 `crowded`가 절반을 넘는다(52개 중
  31개). 그리고 07-31은 `overlap`이 1이다 — 지금 코드가 그 폭에서 라벨 두 개를
  실제로 겹쳐 그리고 있다. 데스크톱에서는 네 날 모두 0이므로 이건 좁은 화면
  분기(`NARROW_HEIGHT_PER_WORD`)가 주는 높이가 모자라다는 뜻이고, 높이를 패킹이
  돌려주게 되면 사라져야 할 값이다.
- **엣지 길이 중앙값이 80~88px**인데 라벨 하나가 그보다 넓은 경우가 흔하다. 선이
  짧아서 안 보이는 게 아니라, 짧은 선이 남의 사건을 관통하고 있어서 안 보인다.

통과 조건: **네 날 모두에서 `crowded`·`crossings`가 내려가야 한다.** 안 내려가면
설계가 틀린 것이다. `overlap`은 어디서도 0을 넘으면 안 된다.

## 사건 구역 배치 이후

> **아래 표는 이미 낡았다.** 2026-08-03 오후에 수집이 하루 1회에서 6회로 늘면서
> 그날이 900건에서 2,197건이 됐고, 그리는 70단어와 엣지 집합이 통째로 바뀌었다.
> 최신 수치는 이 절 다음의 "수집 확대 이후"를 볼 것. 이 표는 **배치 재작성의
> 전후 비교**로서만 유효하다 — 그 비교는 같은 단어·같은 엣지 위에서 이뤄졌다.

```
   view         day  nodes  edges  drawn  crowded  crossings  xIn  xBr  bridges  lenMed  lenMax  overlap   height
-------  ----------  -----  -----  -----  -------  ---------  ---  ---  -------  ------  ------  -------  -------
desktop  2026-07-31     70     37     37        0          1    0    1        3      97     240        0   772.91
desktop  2026-08-01     70     34     34        1          1    0    1        1      84     739        0   738.05
desktop  2026-08-02     70     52     52        6         16   16    0        1      90     228        0   653.86
desktop  2026-08-03     70     58     58        8         21   18    3        2     104     540        0   789.59
  phone  2026-07-31     70     37     37        2          2    0    2        3      97     246        0  1740.56
  phone  2026-08-01     70     34     34        0          0    0    0        1      84     169        0  1848.85
  phone  2026-08-02     70     52     52        6         16   16    0        1      90     228        0  1491.77
  phone  2026-08-03     70     58     58        9         20   18    2        2     104     337        0  1934.06
```

**`crowded`는 여덟 칸 전부에서 내려갔다.** 데스크톱 7·2·10·10 → 0·1·6·8, 폰
18·21·31·30 → 2·0·6·9. 폰이 특히 그런데, 358px에서 절반 넘는 선이 글자 밑을
지나가던 것이 이제 한 자릿수다. `overlap`은 전부 0이고, 기존에 폰 07-31에 있던
겹침 1쌍도 사라졌다 — 높이를 폭에서 지어내지 않게 되면서 원인 자체가 없어졌다.

**`crossings`는 여덟 칸 중 다섯에서 내려갔고 셋에서 올랐다.** 통과 조건을 글자
그대로는 못 채웠다. 오른 셋이 어디서 왔는지가 중요하다:

- **데스크톱 08-01: 0 → 1.** 선이 34개뿐이라 예전 전역 시뮬레이션이 우연히
  교차 없는 배치를 찾아냈던 날이다. 늘어난 1개는 다리(`xBr` 1)다.
- **데스크톱 08-03: 19 → 21, 폰 08-03: 13 → 20.** `xIn`이 18이다.

즉 남은 난잡함은 **거의 전부 하루의 제일 큰 사건 하나 안에** 있다. 08-02는
16개가 전부 `xIn`이고 `xBr`은 0이다. 13단어 27엣지짜리 덩어리(전당대회)를
힘 균형으로 펴는 데는 한계가 있고, 자리 맞바꿈(`untangle`)으로도 더 안 줄었다 —
단쌍 교환은 그 크기의 조밀 그래프에서 곧바로 지역 최적에 걸린다.

**이건 설계가 틀렸다는 뜻이 아니라 재는 자가 한 겹 거칠었다는 뜻이다.** 서로
무관한 두 이야기의 선이 엇갈리는 것과, 한 이야기 안에서 선이 엇갈리는 것은
읽는 사람에게 같은 사건이 아니다. 앞의 것은 없어졌고(`xBr`이 큰 두 날에서 0),
뒤의 것이 남았다. 다음에 이 숫자를 더 내리고 싶다면 손댈 곳은 배치가 아니라
**조밀 사건 하나를 평면에 가깝게 그리는 방법**이다.

`lenMax`는 사건 구역을 나누면 다리가 길어져 208 → 719까지 뛰었다가,
`orderForPacking`이 다리로 이어진 사건을 서로 옆에 놓게 하면서 240·228·337로
돌아왔다. 데스크톱 08-01의 739는 남아 있다 — 그 날은 다리가 하나뿐이라 순서를
바꿀 여지가 없다.

## 수집 확대 이후 (2026-08-03, 크론 6회로 늘린 뒤)

```
   view         day  nodes  edges  drawn  crowded  crossings  xIn  xBr  bridges  lenMed  lenMax  overlap   height
-------  ----------  -----  -----  -----  -------  ---------  ---  ---  -------  ------  ------  -------  -------
desktop  2026-07-31     70     36     36        0          1    0    1        3      99     240        0   708.11
desktop  2026-08-01     70     33     33        1          3    0    3        2      87     703        0   732.05
desktop  2026-08-02     70     50     50        6         16   16    0        1      92     228        0   650.95
desktop  2026-08-03     70     72     72        6         28   14   14        7      87     404        0   865.37
  phone  2026-07-31     70     36     36        2          2    0    2        3      99     246        0  1740.56
  phone  2026-08-01     70     33     33        0          0    0    0        2      87     172        0  1811.23
  phone  2026-08-02     70     50     50        6         16   16    0        1      92     228        0  1484.69
  phone  2026-08-03     70     72     72        8         18   14    4        7      87     513        0  1930.66
```

**08-03만 움직였고, 다른 세 날은 한 글자도 안 움직였다.** 그날만 다시 수집됐기
때문이다 — 900건에서 2,197건. 세 날은 대조군 노릇을 하므로 이 표는 배치가 아니라
**데이터가 두꺼워졌을 때 배치가 어떻게 버티는지**를 보여준다.

- 엣지가 51에서 72로 늘었고 다리가 2개에서 **7개**가 됐다. 하루가 두꺼워지면
  사건 수가 아니라 사건 사이의 연결이 먼저 늘어난다.
- `overlap`은 여전히 0이고 엣지도 전량 그린다. 분리가 힘의 균형이 아니라 패킹에서
  나오므로 밀도가 올라가도 무너지지 않는다는 것이 이 설계의 주장이었고, 그
  주장이 처음으로 두 배 밀도에서 시험받았다.
- `xBr`가 0에서 14로 뛰었다. 다리가 7개면 `orderForPacking`의 탐욕적 순서로는
  전부를 이웃에 붙일 수 없다. **여기가 다음에 손댈 곳이다** — 구역 안(`xIn` 14)과
  다리(14)가 이제 반반이라, 지금까지 "남은 난잡함은 조밀 사건 안에 있다"였던
  진단이 두꺼운 날에는 더 이상 참이 아니다.

  > **바로 위 문장의 후반부는 틀렸다.** `xBr`가 14인 것은 맞지만 그 원인이
  > `orderForPacking`이라는 것이 틀렸고, 다음 절이 그것을 재서 뒤집는다. 남겨
  > 두는 이유는 이 표가 여기까지밖에 말해주지 않기 때문이다 — 총계 한 열을
  > 보고 원인을 짐작한 자리가 여기다.

## `xBr`를 쪼개 보면 순서 문제가 아니다

`xBr`는 "다리가 낀 교차"라는 한 덩어리인데, 그 안에 성격이 다른 세 가지가 섞여
있었다. 다리 하나하나를 찍어 셋으로 갈랐다:

| 칸 | 다리×다리 | 다리×**자기** 구역 내부선 | 다리×남의 구역 내부선 |
| --- | --- | --- | --- |
| desktop 07-31 | 0 | 1 | 0 |
| desktop 08-01 | 0 | 0 | 3 |
| desktop 08-02 | 0 | 0 | 0 |
| desktop 08-03 | 0 | **14** | 0 |
| phone 07-31 | 0 | 2 | 0 |
| phone 08-01 | 0 | 0 | 0 |
| phone 08-02 | 0 | 0 | 0 |
| phone 08-03 | 1 | 3 | 0 |

**다리끼리 엉킨 것은 여덟 칸 통틀어 1개다.** 남의 구역을 가로지른 것도 3개뿐이고
그 셋은 전부 데스크톱 08-01의 박지원—한동훈 하나에서 나온다(703px, 다리가 둘뿐인
날이라 순서를 바꿀 여지가 없다고 위에 적어둔 그 다리다). 구역이 실제로 다른 구역의
상자를 관통당한 횟수(`overRegions`)를 세도 여덟 칸에서 다리 44개 중 4개뿐이다.

남은 것이 전부, 그리고 두꺼운 날에는 압도적으로 **다리가 자기 구역의 내부 선을
자르고 나간 것**이다. 민주당이 12단어짜리 전당대회 덩어리 한가운데 앉아 있으면
거기서 나가는 선은 어느 쪽으로 가든 그 사건의 바큇살을 가로지른다. 구역을 어떤
순서로 늘어놓아도 이건 안 바뀐다.

**총계 한 열은 원인을 말해주지 않는다.** `xIn`/`xBr`로 가른 것이 "무관한 두
이야기가 엇갈리는가"를 답해준 것처럼, `xBr` 안에서 한 겹 더 갈라야 "순서 문제인가
자리 문제인가"가 답해진다. 이 저장소가 체 하네스에서 배운 것과 같다 — 비율 하나로는
어느 설정이 왜 이겼는지 알 수 없다.

## 거울 뒤집기 (`faceBridges`)

고친 방법: 패킹이 끝난 뒤 각 구역을 **자기 상자 안에서 거울처럼 뒤집어**, 다리를
든 단어가 상대 구역을 바라보는 쪽에 오게 한다. 네 가지(항등·좌우·상하·둘 다)를
재서 다리 길이 합이 제일 작은 것을 고르고, 상대도 뒤집히므로 아무 상자도 안
움직일 때까지 돈다.

**이 지렛대를 고른 것은 측정이 아니라 성질 때문이다.** 거울 반사는 등거리변환이라

- 상자의 크기가 그대로다 → 패킹을 다시 할 필요가 없다.
- 구역 **안**의 교차수와 라벨 겹침을 한 개도 바꿀 수 없다 → `xIn`과 `overlap`은
  건드릴 수 없는 것이 보장된다. 아래 표에서 그 두 열이 여덟 칸 전부 그대로인 것은
  운이 아니라 이 성질이다.

즉 이 문제에만 듣고 다른 것은 건드릴 수 없는 지렛대라서, 회귀를 걱정할 자리가
구조적으로 없다.

```
   view         day  nodes  edges  drawn  crowded  crossings  xIn  xBr  bridges  lenMed  lenMax  overlap   height
-------  ----------  -----  -----  -----  -------  ---------  ---  ---  -------  ------  ------  -------  -------
desktop  2026-07-31     70     36     36        0          0    0    0        3      99     216        0   708.11
desktop  2026-08-01     70     33     33        1          3    0    3        2      87     703        0   732.05
desktop  2026-08-02     70     50     50        6         16   16    0        1      92     228        0   650.95
desktop  2026-08-03     70     72     72        6         19   14    5        7      87     401        0   865.37
  phone  2026-07-31     70     36     36        0          0    0    0        3      99     154        0  1740.56
  phone  2026-08-01     70     33     33        0          0    0    0        2      87     186        0  1811.23
  phone  2026-08-02     70     50     50        6         16   16    0        1      92     228        0  1484.69
  phone  2026-08-03     70     72     72        9         19   14    5        7      87     497        0  1930.66
```

**`xBr` 합이 24에서 13.** 진단이 가리킨 칸에서 제일 크게 내려갔다 — 데스크톱
08-03이 14 → 5, 07-31 두 칸이 1·2 → 0·0. `xIn`은 여덟 칸 전부 그대로고 `overlap`은
여전히 0이다.

**후퇴가 한 칸 있고 감추지 않는다.** 폰 08-03이 4 → 5(그리고 `crowded` 8 → 9)다.
쪼개 보면 다리×다리 1개가 없어지는 대신 자기 구역 자르기가 3 → 5로 늘었다.
**비용이 교차수가 아니라 다리 길이이기 때문이다** — 좁은 폭에서 한 다리를 짧게
만드는 뒤집기가 다른 다리의 출구를 더 많은 바큇살 위로 끌고 갈 수 있다. 비용을
교차수로 바꾸면 이 한 칸은 고쳐지겠지만, 뒤집을 때마다 내부 선의 기하를 다 재야
하고 얻는 것은 여덟 칸 중 한 칸의 1개다. 하지 않았다.

수렴은 **1회**에 끝난다 — `FACE_ROUNDS`를 1·2·3·4·8로 돌려도 네 날의 숫자가 전부
같다. 4는 튜닝값이 아니라 여유다.

남은 것(08-03 두 뷰의 5개씩)은 여전히 "다리 × 자기 구역 내부선"인데, 한 단어가
서로 반대 방향의 다리를 둘 들고 있는 경우다 — 뒤집기 하나로 둘을 다 만족시킬 수
없다. 여기서 더 내리려면 상자 **안에서 그 단어를 옮겨야** 하고, 그건 `xIn`을
올릴 수 있는 도구다. 지금 도구는 못 올린다는 것이 장점이었으므로, 그 교환은
별도로 재고 들어갈 일이다.

## 통과 조건 (고쳐 씀)

원래 조건은 "네 날 모두에서 `crowded`·`crossings`가 내려갈 것"이었고, 사건 구역
재작성이 `crowded`는 여덟 칸 전부에서 내렸지만 `crossings`는 셋에서 올렸다. 총계로는
실패인데 그린 그림은 나아졌다 — 조건이 잘못 쓰인 것이다.

**총계 `crossings`로 판정하지 않는다.** 대신:

- `overlap`은 **어디서도 0을 넘지 않는다.** 이건 불변식이라 예외가 없다.
- `crowded`는 내려가거나 그대로다.
- `xIn`과 `xBr`을 **따로** 본다. 무관한 두 이야기가 엇갈리는 것(`xBr`)과 한
  이야기 안에서 엇갈리는 것(`xIn`)은 읽는 사람에게 같은 사건이 아니다.
- `xBr`를 움직이겠다고 나섰으면 **셋으로 쪼갠 표까지** 붙인다. 총계가 내려간
  것만으로는 원인을 맞혔는지 알 수 없고, 이 절의 앞부분이 그 증거다.
