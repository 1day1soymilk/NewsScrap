# 다음에 할 일

새 세션이 이 파일 하나만 읽고 바로 시작할 수 있도록 쓴 것이다. 배경을 다시
파내지 않아도 되게 **어디를 고칠지와 왜 그런지까지** 적어 둔다. 끝난 항목은
지우지 말고 "닫힘"으로 표시할 것 — 이미 재 본 것을 다시 재는 것이 이 저장소가
반복해서 치른 값이다.

배치 쪽 판단만 따로 모아 둔 `scripts/layout/OPEN.md`는 그대로 살아 있다. 이
파일은 그보다 위, 프로젝트 전체의 대기열이다.

기준 시점 **2026-08-08**, `main` = `2c27402` + 이 브랜치 세 커밋,
게이트 전부 초록 (빌드 0 / 단위 388 / e2e 52).

```bash
npm run build && npx vitest run && npm run lint && npm run test:e2e
```

CLAUDE.md는 **140,016자 / 150,000 한도**다. 이번 세션이 136,643에서 3.4k 늘렸고,
평면화 절은 산문을 `scripts/layout/README.md`로 보내면서 오히려 줄었다. 다음에
한 절을 통째로 더할 여유는 있지만 두 절은 없다 — 늘리기 전에 세어 볼 것.

---

## 열려 있는 것

**둘 다 관측이고, 코드는 안 기다린다.**

### 1. 크론 열둘의 첫날을 읽을 것 (2026-08-09)

수집을 4시간 간격 여섯 번에서 2시간 간격 열둘 + 23:50로 바꿨다. 다음 날 확인할 것:

```sql
select h.collected_date,
       to_char(date_trunc('hour', h.created_at at time zone 'Asia/Seoul'), 'HH24:MI') as run_kst,
       count(*) as new_rows
from headlines h where h.collected_date = current_date group by 1,2 order by 2;
```

- **모든 런이 행을 남겼는가.** 워커 CPU는 누적이라 2시간 간격이 안전하다고
  보지만 그것은 추론이지 측정이 아니다. 빈 런은 546(본문 없는 응답)이다.
- **23:50 런이 예전에 잃던 ~100건을 잡았는가.**
- **하루 총량이 얼마나 올랐는가.** 오르면 그것이 세 번째 수집 체제 경계의 크기다.

### 2. `RIPE_SHARE` 0.28을 그날 표로 확정할 것

`src/lib/openingDate.ts`. 캔버스가 어느 건수에서 70개를 다 그리는지를 재서
정했는데, 관측점이 아카이브의 **날 단위** 여섯 개였다(풀 221 → 36노드, 373 → 68,
530 → 70, 605 → 70). 크론이 열둘이 됐으므로 이제 **하루 안에서** 열두 점이 나온다:

```sql
select g.collected_date, json_array_length(g.graph->'nodes') as nodes, d.headline_count
from keyword_graph_cache g join collected_dates d on d.collected_date = g.collected_date
where g.category_slug is null and g.collected_date = current_date;
```

0.28은 고원(07:00이 0.19, 11:00이 0.35~0.40) 한가운데라 웬만해선 안 움직일 것이다.
움직인다면 `openingDate.test.ts`의 마지막 테스트가 그 근거를 들고 있으니 같이 고칠 것.

---

## 그 밖에 다시 열 만한 것

**없다.** 아래 4절(평면화 예산의 단위)이 이번에 닫혔다.

---

## 닫힌 것 — 다시 열지 말 것

### Z. 아침에 기사가 없다 — **닫힘 (2026-08-08)**

원인이 둘로 갈렸고 처방도 둘이었다. 실측(08-05·08-06·08-07):

| KST | 누적 헤드라인 | `df>=3` 풀 |
| --- | --- | --- |
| 03:00 | 198 / 209 / 237 | **59 / 81 / 74** |
| 07:00 | 592 / 572 / 587 | 269 / 284 / 269 |
| 11:00 | 1224 / 1196 / 1160 | 662 / 670 / 646 |
| 하루 끝 | 3077 / 3077 / 3317 | 1640 / 1670 / 1836 |

- **03:00엔 풀이 `render_cap` 70보다 작다.** 캔버스가 찰 수가 없고, **수집을
  고쳐서 닿을 수 있는 자리가 아니다** — 07:00 전에 150건을 발행한 섹션이 하나도
  없다. 날짜 경계 정지는 옳게 돌고 있고 그냥 기사가 없다. → **앱이 채워진 직전
  날로 연다.**
- **화면이 발행을 최대 4시간 뒤처져 따라갔다.** 07:00 다음이 11:00인데 그 사이
  하루가 두 배가 된다. → **크론 2시간 간격 + 23:50.**

**덤으로 구멍 하나를 찾았다.** 각 날의 마지막 행이 23:00:07이었다 — 23시~자정
발행분은 03:00 런이 off-day로 버려서 하루 ~100건이 영구히 안 들어오고 있었다.

**문턱은 그려진 캔버스에 대고 쟀다.** 풀 221 → 36노드, 373 → **68**, 530 → 70,
605 → 70. 즉 캔버스는 풀 450쯤에서 차고, 채워지는 날은 헤드라인 ~860건에서 거기
닿는다. 보통 날의 **0.28**이고 07:00(0.19)과 11:00(0.35~0.40) 사이 고원 한가운데다.
건수가 아니라 비율인 것은 수집 깊이가 이 아카이브에서만 세 번 바뀌었기 때문이고,
평균이 아니라 중앙값인 것은 4,218건짜리 날이 하나 있기 때문이다.

**테스트 둘이 못 실패하는 상태였다는 것이 이 항목의 소득이다.** "링크가 규칙을
이긴다"는 e2e가 가드를 지워도 통과했다 — 날짜 입력은 첫 렌더부터 오늘을 들고
있고 자동 재시도 단언은 참이 되는 순간 돌아오므로, `collected_dates`가 도착하기도
전에 끝난다. 요청된 날을 기록해서 판정하고 `networkidle`을 장벽으로 쓴다.
그리고 **뒤로가기 한 번은 replaceState와 pushState를 구별 못 한다** — push면
스택이 [오늘, 어제, 오늘]이라 첫 번째 뒤로가기도 어제에 닿는다. 두 번째가 판정한다.

`fetchCollectedDates`가 캐시로 들어간 것은 정리가 아니라 수정이다. 주석이
"마운트에서 한 번 읽는다"였는데 부르는 곳이 둘이 되면서 거짓이 됐고, 찬 로드에서
**세 번** 나가고 있었다. 찬 로드 요청 8 → 7(오늘을 보는 경우), 8 → 9(아침).

### A. `30_word_scores.sql`이 거짓 `!`를 띄운다 — **닫힘 (2026-08-08)**

그 파일이 스스로 예고한 대로였다: *"게이트를 켜는 뒤 라운드는 여기 절을 손으로
넣어야 한다. 이 교차검사는 그걸 요구하지 않기 때문이다."* `0028`이 켰고 아무도 안
넣어서, `서울`(mode `place`, 라벨 good)이 `cut: rank`에 `!`를 달고 나왔다.

**게이트를 다시 구현하지 않았다.** `keyword_graph_rank(cands, '{}')`가 출하된
순위 헬퍼에 빈 금지 목록을 준 것이고, 빈 금지 목록이 곧 게이트-오프 화면이다.
그 순위에 있으면서 안 그려진 place는 sieve 6이 가져간 것 말고는 될 수가 없다 —
금지는 살아남은 것을 밀어 올리기만 하므로. 논증이 아니라 실측으로 확인했다:
다섯 날에서 게이트-오프에 있고 안 그려진 단어가 2/2/1/6/3개인데 **그중 place가
2/2/1/6/3개**, 즉 전부다.

`gate_dropped` 불리언 하나가 판정과 `chk` 예외를 동시에 먹이므로 둘이 다른 답을
할 수 없다. `!`는 07-31·08-02·08-03과 오늘의 얇은 08-08에서 전부 0이다.

### B. 레이아웃 픽스처가 게이트보다 오래됐다 — **닫힘 (2026-08-08)**

다시 떴다(2026-08-08 10:33 KST). 그리고 결과가 예상과 달랐다.

**게이트가 지운 열두 단어는 전부 선을 안 가진 단어였다.** `crowded`·`crossings`·
`xIn`·`xBr`·`bridges` 다섯 열이 열두 칸에서 한 자리도 안 움직였다. 게이트의 전제
("선 없는 place는 배경이다")를 라벨이 아니라 **캔버스 위에서** 확인한 셈이다.
오히려 엣지는 늘었다 — 07-31이 45 → 47, 08-03이 63 → 66. 빈 자리를 선을 가진 더
깊은 단어가 채우기 때문이다.

08-02의 13단어 사건은 안 움직였으므로 4절(평면화 단가)은 재측정 없이 그대로 선다.
유일한 후퇴는 phone 08-03의 `overBoxes` 0 → 1이고 표에 적어 뒀다.

두꺼운 날(08-04)은 `graphDays.fat.json`으로 따로 잰다. **엣지 45개로 제일 성긴
날이다** — 70자리를 더 많은 이야기가 나눠 갖고 NPMI가 흔한 단어의 짝을 깎기
때문. 표는 `scripts/layout/README.md`.

### C. `11_category_eval.sql`에는 sieve 6이 없다 — **닫힘 (2026-08-08)**

한계는 그대로고, 이제 **그 파일 자신의 헤더에** 적혀 있다. `OPEN.md`에만 있으면
그 파일을 여는 사람은 못 읽는다.

그리고 게이트 켠 탭 숫자를 처음으로 뽑아 남겼다 — `29_gate_on_category.sql`,
42셀에서 F1 68.44 / 정밀도 76.69, world 78.65에서 society 58.50까지. 지표 공식은
`11`의 것을 그대로 쓴다. **두 표를 나란히 놓지 말 것** — 서로 다른 화면이다.

그 파일이 세 번째 워크리스트를 요구했다는 것이 덤으로 나온 소득이다. 게이트는
place를 지우면서 그 자리에 더 깊은 단어를 **올리므로**, 게이트 없는 워크리스트
둘 중 어느 것도 그 화면을 못 본다. `23_unlabeled_gate_on.sql`이 그것이다.

### D. 게이트된 α를 배포에 싣기 — **닫힘 (2026-08-08). 마이그레이션 `0035`.**

라운드 16이 잰 것: **α 1.00을 그날의 불균형(spread ≥ 1.2)에서만 켜면 일곱 날 중
한 날도 안 진다.** 셋이 오르고 넷이 같다. F1 50.16 → 50.51, 정밀도 86.49 → 87.30.
평평한 α는 균형 잡힌 날에 3.0을 내주고 사는 것이라 이쪽이 **엄격히 우세**하다.

**게이트는 α가 이미 해석되던 한 곳**(`category_balance_factors`의 `alpha` CTE)에
들어갔고 **기본 경로에만** 적용된다 — 명시적 `p_alpha`는 그대로 존중한다. 하네스가
α별 슬라이스를 만들어 스스로 고르기 때문이다. 그날의 불균형은
`category_balance_spread(d)`이고 **α와 무관하게** 정의된다: 자기가 켜는 지수를
통해 재면 α가 0인 동안 모든 날이 1.0으로 읽혀 스스로를 못 켠다.

화면에서 실측: **63칸 중 55칸 바이트 동일.** 균형 잡힌 07-31은 일곱 뷰 전부 동일,
그리고 **카테고리 탭 48칸이 전부 동일** — α가 탭 안에서 항등이라는 구성상 성질이
논증이 아니라 측정으로 확인됐다. 움직인 것은 불균형한 여덟 날의 `ALL` 뷰뿐이다.
08-07에서는 서울·오피스텔·주택공급·정비사업(society 989건)이 물러나고
컴투스·삼성전자·테슬라·쿠팡·오픈AI·메타(it 262건)가 들어온다.

**같은 숨에 하네스의 출하 행도 옮겼다**(320). 그리고 그 규칙이 사람의 기억에
기대지 않도록 `32_shipped_row_agreement.sql`을 만들었다 — `0028`이 실패한 검사의
자동화된 형태다. knob 11개 전부 일치.

**딸려 나온 것 하나.** 그 검사를 단어 단위로도 한 번 돌렸더니 08-03에서 하네스가
`강원`을, 배포가 `데이터센터`를 그린다. 하네스의 sieve 6이 단일 패스라
"빈 자리로 승격된 단어를 다시 검사하지 않는" 것인데, 이건 그 파일 주석이 미리
적어 둔 근사다. **α가 드러낸 것이지 만든 것이 아니다** — 게이트를 끄고 재니 일곱
날 전부 0 차이. 70개 중 하나, 일곱 날 중 하루라 고치지 않았고, 고치면 한 파일 안에
sieve 6의 두 번째 사본이 생긴다. 이유는 `32`의 헤더에 있다.

### 라운드 16이 찾은 구멍 셋 — 같은 종류다

전부 **파생된 것이 파생의 근거를 안 따라간 것**이고, 전부 워크리스트가 아니라
하네스의 `unlab` 열이 잡았다.

- `analysis.day_edges`가 `analysis.eval_days`를 안 따라갔다. 라운드 15가 08-04을
  넣고 이 표를 안 지어서, 그날 게이트 설정이 place 셋을 전부 떨어뜨렸다. 45칸 중
  6칸이 단어 하나씩 움직이고 **출하 행은 안 움직인다**(55.88 그대로). 이제
  `12_eval_days.sql`이 날 목록 바로 아래서 짓는다.
- `21_unlabeled_category.sql`이 `11_category_eval.sql`을 안 따라갔다. 라운드 15가
  head_pos 축을 하네스에만 넣었다. 하네스가 `unlab` 6을 찍는데 워크리스트는 0행이었다.
- 게이트 켠 화면을 따라가는 워크리스트가 아예 없었다 (위 C항).

**규칙:** 하네스가 재는 화면마다 같은 화면을 그리는 워크리스트가 있어야 하고,
빠진 것을 찾아 주는 것은 워크리스트가 아니라 하네스의 `unlab`이다. **빈
워크리스트는 워크리스트에 대한 증거다.**

### 그보다 앞서 닫힌 것 — 같은 날의 앞 세션들

### 0. CLAUDE.md가 한도를 넘었다 — **닫힘 (2026-08-08)**

150,000자 한도에 153,120자였다. 넘친 부분은 조용히 잘리므로 꼬리에 있던
"Testing notes"와 "Word quality is measured"가 안 읽히고 있었다 — 하필 규칙 4와
뮤테이션 검증 습관을 담은 절들이다.

**126k로 내렸고, 한 것은 이사가 아니라 중복 제거다.** 긴 절들이 이미
`scripts/analysis/README.md`와 `scripts/layout/README.md`에 더 자세히 살아
있었다. 금지문과 그 이유 한 줄만 남기고 측정 서사를 지운 뒤 포인터를 걸었다.
산문 사본이 CLAUDE.md에만 있던 수집기 쪽은 새 집으로 옮겼다 —
`supabase/functions/collect-headlines/README.md`.

기준은 하나였다: **그 문장을 안 읽고도 실수를 안 하는가.** 실수를 막는 것은
금지문이고, 숫자는 그 금지문을 움직이려 할 때 읽는 것이다.

### 1. 헤드라인 패널의 날짜 칸이 접히지 않는다 — **닫힘 (2026-08-08)**

열린 줄을 다시 눌러도 안 닫히고, 전부 닫힌 상태를 만들 수 없었다. `App.tsx`가
열린 날을 문자열 하나로 들고 있어서 같은 날을 눌러도 값이 안 바뀌었다 — 집합이
아니면 토글도 빈 상태도 표현이 안 된다.

집합으로 바꾸면서 형이 정한 것 둘을 같이 넣었다: **기본은 전부 닫힘**(그래서
단어를 눌러도 헤드라인 요청이 안 나간다. 예전에는 화면의 날짜 한 번이 무조건
나갔다), 그리고 **기사가 없는 날은 줄 자체를 안 그린다**(8월 3일 아래 바로
8월 1일). 두 번째는 코드를 줄였다 — 비활성 분기, "기사 없음" 표식, 열린 빈 날을
위한 예외가 통째로 사라졌다.

두 자리가 까다로웠고 둘 다 기록해 뒀다. 날짜별 목록과 궤적은 **각각 무엇에 대한
것인지와 짝지어** 들고 다닌다(`partition`이 `graph`와 짝지어 다니는 것과 같다).
그리고 "궤적이 도착했나"는 **한 번만** 묻는다 — 두 군데서 물었더니 한쪽을 지워도
다른 쪽이 가려 줘서 뮤테이션으로 안 죽었다.

게이트를 하나씩 되돌려 **의도한 테스트만** 깨지는 것을 넷 다 확인했다.

### 2. `head_pos` 컷 vs 강등 — **닫힘 (2026-08-08). 강등 그대로.**

두꺼운 날이 없어서 막혀 있었다. 2026-08-04를 eval day로 넣고 재니 **컷은 탭에서
여전히 진다 — cap이 걸리는 그 두꺼운 날에도.** 6탭 중 5탭이 cap에 찬 08-04에서
컷 50.18, 강등 50.88. 일 단위로는 컷이 0.36 이기고 탭에서 0.42 잃는, 라운드 5와
똑같은 서명이다.

**예측이 틀린 이유가 이 라운드의 소득이다.** `shown`이 강등 69.2, 컷 67.8이다 —
**컷은 cap이 걸리는 것에 기댈 수 없다. 걸리지 않게 만드는 것이 컷 자신이기
때문이다.** 일반형: 후보를 없애는 장치는 그것이 가능하게 하는 치환으로 정당화될
수 없다. 그 치환이 기대는 여유분을 같이 쓰고 있기 때문이다.

### 3. α(`balance_alpha`) 재측정 — **닫힘 (2026-08-08). 평평하게는 안 켠다.**

> **라운드 16이 이 항목의 마지막 문단을 뒤집었다.** 아래는 라운드 15의 기록이고
> 그 측정은 그대로 유효하다 — 평평한 α는 지금도 진다. 바뀐 것은 "게이팅은 문턱
> 하나를 살 값이 못 된다"는 판단이고, 두 날이 더 쌓이고 하네스가 게이팅을 **설정으로
> 돌릴 수 있게** 되자 그것이 한 날도 안 지는 개선으로 측정됐다. 위의 열려 있는
> 1항이 그 후속이다.

α는 모든 설정에서 진다. **그리고 손해 전부가 교정할 것이 없는 한 날에 몰려
있다** — 2026-07-31은 여섯 섹션이 한 번의 capped run으로 150/149/150/150/150/150을
받아 균형 계수가 0.999~1.006이다. 거기서 α는 `df` 동점을 소수 셋째 자리에서
흔드는 것 말고 하는 일이 없고, 좋은 단어 셋을 잃는다. 불균형이 실제로 있는 세
날(1.49~2.52)에서는 중립이거나 **이득**이고, 여기에는 α가 만들어진 그 날
2026-08-04이 포함된다.

**즉 α가 틀린 것이 아니라 필요 없는 날에까지 거는 것이 틀렸다.** 그날의 불균형에
따라 켜면 산술로 56.14/87.92(출하 55.88/87.36)인데, +0.26 F1은 스스로 튜닝이
필요하고 영구적인 규칙 4 의무가 붙는 문턱 하나를 살 값이 못 된다.

### 4. `PLANAR_AREA_PER_CROSSING` — **닫힘 (2026-08-08). 1.0 그대로.**

`scripts/layout/OPEN.md` 2절. **하네스가 값을 못 보고 있었다** — `width` 열이
없었다. svg는 제 크기로 그려진 뒤 균일 축소되므로 폭이 커지면 글자가 같이
작아지는데, 표는 높이만 찍고 있었다.

열을 붙이고 재니: k를 1.3 이상으로 올리면 08-02의 그 사건이 평면으로 그려지고
`xIn`이 40 → 7이 되지만, **그려지는 svg가 세 뷰 모두 2,864px 폭**이 된다 —
phone에서 배율 0.125라 최소 글자 14px가 화면에서 1.8px다. **교차 33개를 지우고
그날 69개 단어를 전부 잃는다.**

원인도 기록과 달랐다: 후보 12개 중 열 개는 겹침에서 먼저 탈락하고, `forced`는
40이 아니라 52라 예산은 이미 46배를 허용한다. 다음에 고칠 자리는 상수가 아니라
**예산이 재는 대상**이다 — 지금은 힘 배치 넓이 대비이고, 값을 치르는 것은 뷰의
폭 대비다.

### 4′. 그 "다음에" — **닫힘 (2026-08-08). `PLANAR_PX_PER_CROSSING` 0.1.**

넓이 예산을 지우고 **교차 하나당 화면에서의 최소 글자 몇 px**로 바꿨다. 상수가
아니라 단위를 바꾼 것이라 위 문단이 요구한 그대로다.

**첫 설계는 절대 문턱이었고 하네스가 바로 부러뜨렸다.** `minPx` 열을 붙이자마자
나온 것은 평면 경로와 무관한 사실이었다 — **phone은 이미 5.8~8.8px로 그려지고
있다**(구역들이 선반에 담기며 캔버스가 358보다 넓어져서). "화면에서 N px 이상"
같은 바는 지금 그림을 통째로 부정한다. 값을 매길 수 있는 것은 **이 장치가 가져가는
px**뿐이다.

쓸기는 여전히 절벽이지만(0.0~0.2 동일, 0.5에서 전부 열림) **이번엔 열이 둘이다** —
교차는 k에 대해 단조로 줄고 `minPx`는 단조로 는다. 고르는 것은 여전히 판단이지만
표가 사는 것과 파는 것을 같이 찍는다. 0.1은 고원 한가운데다.

값: **desktop과 wide는 모든 칸 동일**, phone만 움직인다. 08-03이 교차 27개를
내주고 글자 5.8 → 13.4px, 높이 −39%. 두꺼운 날은 모든 열에서 나아진다. **옛 예산은
같은 거래를 반대 방향으로, 재는 열 없이 하고 있었다.**

### 그 이전에 닫힌 것

- **그래프 캐시 블록** (마이그레이션 `0032`~`0034`: 캐시, 자가 치유, 설정 지문).
  `bc780b2`로 머지됨. 2026-08-08 기준 `keyword_graph_cache_health`는 9칸 전부
  `current`다.
- **동시 접속자 500** (`anon`의 3초 statement_timeout). `0032`가 닫았다. 캐시
  히트는 지문 검사까지 6.5ms, 계산은 약 2초.
- **패널 행 상한.** `HEADLINE_ROW_LIMIT` 200 → 600. 이 값을 움직이는 것은 단어가
  아니라 `collect_cap`이다.

---

## 이번 세션이 남긴 습관 하나

**`scoring_weights`를 바꾸는 마이그레이션은 하네스의 출하 설정 행도 같은 숨에
바꿔야 한다.** `0028`이 place gate를 켰는데 설정 200은 계속 꺼짐이라, 한 릴리스
주기 동안 하네스가 앱이 안 그리는 화면을 재고 있었다. 라운드 15의 설정 300이 그
교정이고, 이 규칙은 CLAUDE.md에도 적어 뒀다.

## 이번 세션이 남긴 습관 하나 더

**계기가 없으면 그 값은 안 치러지는 것이 아니라 안 보이는 것이다.** 이번에 두 번
같은 모양이었다. 평면화 예산은 `minPx` 열이 생기기 전까지 phone의 글자 크기를
조용히 팔고 있었고, 아침 화면은 `df>=3` 풀을 시간별로 세 보기 전까지 "수집이
얕다"로 읽히고 있었다(실은 기사가 없는 것이었다). 둘 다 **고치기 전에 재는 열부터
만들었더니 진단이 바뀌었다.**

곁가지 하나가 그 자리에서 또 나왔다: 절대 문턱을 걸려던 첫 설계가 새 열 하나에
부러졌다. 지금 그림이 이미 그 바 아래에 있었기 때문이다. **새 문턱을 세울 때는
지금 그림이 그 문턱을 통과하는지부터 볼 것.**

## 라운드 16이 남긴 습관 하나

**파생된 것은 파생의 근거 바로 옆에 두고, 없으면 `unlab`으로 찾는다.** 위의 구멍
셋이 전부 같은 모양이었다 — 날 목록에서 나온 표, 하네스에서 나온 워크리스트,
화면에서 나온 워크리스트가 각각 제 근거를 안 따라갔다. 셋 다 워크리스트는 조용했고
하네스의 `unlab`만 말을 했다. **워크리스트가 비었다는 것은 화면이 다 라벨됐다는
뜻이 아니라, 그 워크리스트가 그 화면을 본다는 전제가 참일 때만 그런 뜻이다.**
