# 다음에 할 일

새 세션이 이 파일 하나만 읽고 바로 시작할 수 있도록 쓴 것이다. 배경을 다시
파내지 않아도 되게 **어디를 고칠지와 왜 그런지까지** 적어 둔다. 끝난 항목은
지우지 말고 "닫힘"으로 표시할 것 — 이미 재 본 것을 다시 재는 것이 이 저장소가
반복해서 치른 값이다.

배치 쪽 판단만 따로 모아 둔 `scripts/layout/OPEN.md`는 그대로 살아 있다. 이
파일은 그보다 위, 프로젝트 전체의 대기열이다.

기준 시점 **2026-08-08**, `main` = `bd56333`, 게이트 전부 초록
(빌드 0 / 단위 369 / e2e 49).

```bash
npm run build && npx vitest run && npm run lint && npm run test:e2e
```

---

## 1. 헤드라인 패널의 날짜 칸이 접히지 않는다 — **버그, 제일 먼저**

**증상.** 열려 있는 날짜 줄을 다시 눌러도 안 닫힌다. 닫으려면 *다른* 날짜를
눌러 옮기는 수밖에 없고, 그래서 **전부 닫힌 상태를 만들 수 없다.**

**원인.** 어젯밤 올린 구현이 "열린 날 하나"를 상태로 들고 있다. `App.tsx`의
`panelDate`가 `{ for, date }` 하나이고 `HeadlinePanel`은 `openDate` 문자열
하나를 받아 `day.date === openDate`로만 펼침을 판정한다. 같은 날을 다시 눌러도
`onOpenDate(같은 날짜)`라 값이 안 바뀐다. 토글도, 여러 개도 애초에 표현이 안 되는
모양이다.

**원하는 동작.** 각 날짜를 독립적으로 여닫는다. 여러 날을 동시에 열 수 있고,
전부 닫을 수도 있다.

**고칠 자리.**

- `src/App.tsx` — `panelDate`를 날짜 **집합**으로 바꾼다. 지금 모양은
  `useState<{ for: string; date: string } | null>`이고, `for`에 어떤 상태에 대한
  펼침인지를 같이 들고 다녀 날짜/단어가 바뀌면 저절로 무효가 되게 해 뒀다 —
  **그 성질은 유지할 것.** 되돌리는 effect를 쓰면 그 한 렌더 동안 이전 날짜가
  유효해 보여서 지난 날의 요청이 한 번 나간다(주석에 적혀 있다). 그러니
  `{ for: string; dates: Set<string> } | null` 정도로 가고, `for`가 안 맞으면
  기본값(화면의 날짜 하나만 열림)으로 읽는다.
- `src/components/HeadlinePanel.tsx` — `openDate: string` → `openDates: Set<string>`,
  `onOpenDate(date)` → `onToggleDate(date)`. `isOpen`은 `openDates.has(day.date)`.

**같이 봐야 하는 것 — 이게 진짜 일이다.** 지금은 열린 날이 하나뿐이라 App이
그 하루치 헤드라인만 받아 `headlines` prop 하나로 내려보낸다. 여러 날이 동시에
열리면 **날짜별로 따로 받아 따로 들고 있어야 한다.** 즉 `headlinesForWord`가
`Map<date, HeadlineSummary[]>`가 되고 로딩/오류도 날짜별이 된다.

한 번에 여러 날을 받아 오는 쪽으로 가지 말 것. 실측(2026-08-08, 라이브):
폭염은 9일에 **952건 / 명사행 973**이라 여러 날을 한 요청으로 받으면 PostgREST의
1,000행 캡에 앉아 **말없이 잘린다.** 하루씩 받는 것은 캡이 구조적으로 못 걸리고
이미 날짜별로 캐시돼 있다(`fetchHeadlinesForWord`, 5분 TTL).

**잊지 말 것 둘.**

- **스켈레톤은 기다릴 것이 있을 때만.** 이미 받아 둔 날은 왕복 없이 풀리는데
  거기에 한 프레임 스켈레톤을 번쩍이면 캐시로 번 것을 그대로 돌려준다.
  판정은 프로미스 순서로 추측하지 말고 `fetchHeadlinesForWord.isReady(...)`로.
  `loadGraph`가 같은 이유로 같은 모양을 하고 있다.
- **열린 칸은 "기사 없음" 표식과 비활성에서 면제된다.** 화면의 날짜에 그 단어가
  없을 수 있고(검색으로 닿으면 흔하다), 펼쳐진 비활성 컨트롤이 되거나 표식과
  본문이 같은 말을 두 번 하게 된다. 지금 테스트가 이걸 지키고 있으니 깨지 말 것.

**테스트.** `src/components/HeadlinePanel.test.tsx`의 `describe('HeadlinePanel 날짜 구획')`
전체가 `openDate` 하나를 전제로 쓰여 있다. 새로 넣을 것:
같은 줄을 두 번 눌러 닫히는가, 두 날을 동시에 열 수 있는가, 전부 닫으면
헤드라인이 하나도 안 남는가. e2e는 `e2e/headlinePanel.spec.ts`의
"기사를 날짜별로 나누고, 다른 날을 열어도 화면의 날짜는 움직이지 않는다".

**고쳤으면 뮤테이션으로 확인할 것.** 이 저장소는 "지워도 통과하는 테스트"에
두 번 당했다. 게이트를 하나씩 되돌려 **의도한 테스트 하나씩만** 깨지는지 본다.

---

## 2. `head_pos` 컷 vs 강등을 두꺼운 날에서 재측정 — **막힘이 풀렸다**

`scripts/layout/OPEN.md` 5절이 "6회 수집 아래 두꺼운 날이 08-03 하나뿐이고 ETRI
키가 막혀 안 쌓인다"고 막아 뒀는데 **그 문장이 낡았다.** 분석기(garu-ko)가 함수
안으로 들어와 키가 필요 없고, 수집일은 이렇게 쌓였다:

| 날 | 07-31 | 08-01 | 08-02 | 08-03 | 08-04 | 08-05 | 08-06 | 08-07 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 헤드라인 | 899 | 1,144 | 691 | 2,197 | 4,218 | 3,077 | 3,077 | 3,317 |

**왜 열려 있나.** CLAUDE.md가 스스로 근거를 무너뜨려 뒀다 — "탭에서는 render
cap이 안 걸리니 컷은 채울 것 없는 손실"이라고 강등을 정당화했는데, 2026-08-03은
여섯 탭 전부가 95~163개 후보를 cap 70에 밀어 넣는다. 두꺼운 날에는 탭에서도
치환이 일어나므로 컷이 다시 후보가 된다.

## 3. α(`balance_alpha`) 재측정 — **막힘이 풀렸다**

`scripts/analysis/24_cap_and_place_configs.sql` 꼬리가 "2026-08-04가 수집을
멈추고 라벨링되면 다시 돌릴 것"이라고 명시해 뒀고 그 날은 닫혔다. 설정 행
220~223이 비활성으로 남아 있어 `active` UPDATE 한 번이면 켜진다.

## 2·3 공통 — **선행 비용은 라벨링이고 그게 진짜 값이다**

규칙 4: eval day를 늘리면 `12_eval_days.sql`에 행을 넣고 `20_unlabeled.sql`과
`21_unlabeled_category.sql`을 돌려 나온 단어를 **전부** 라벨해야 하네스의 어떤
행도 읽을 수 있다. 예전 실측으로 day-wide 21~26개/날, 카테고리 쪽은 과거에
232~234개였다. 라벨은 정답 데이터라 판단이 들어가고, 운용 기준은 CLAUDE.md에
적혀 있다 — *"이 단어가 아무 다른 주의 뉴스에도 나올까?"*

**날 고르는 것 자체가 판단이다.** 2026-08-07이 수집 상한 150→300 경계라 그
전후 날을 F1으로 직접 비교하면 안 된다(화면은 70 고정인데 회수율 분모가 커진다).
하네스 안에서 설정끼리 비교하는 것은 그대로 유효하다.

## 4. `PLANAR_AREA_PER_CROSSING` 판단 — 열림

`scripts/layout/OPEN.md` 2절. 2026-08-02의 13단어 사건 하나가 면적 예산에
거절당해 바닥값 2에 대해 교차 40개를 낸다. 단가가 다이얼이 아니라 절벽이라
사이값이 없고, 하네스가 정해 줄 수 있는 문제가 아니다.

## 5. 형이 아직 안 말한 항목

"그것들도 다 할 건데 또 있어"라고 했고 내용은 아직 안 받았다. **아침에 먼저
물어볼 것.**

---

## 닫힌 것 — 다시 열지 말 것

- **그래프 캐시 블록** (마이그레이션 `0032`~`0034`: 캐시, 자가 치유, 설정 지문).
  `bc780b2`로 머지됨. 지문 불일치 자가 치유는 캐시 행을 일부러 망가뜨려 사슬
  전체를 돌려 확인했고, 2026-08-08 기준 `keyword_graph_cache_health`는 9칸 전부
  `current`다. `docs/DEPLOYMENT.md`의 수동 갱신 지시도 지워졌다.
- **동시 접속자 500** (`anon`의 3초 statement_timeout에 걸리던 문제). `0032`가
  닫았다. 캐시 히트는 지문 검사까지 포함해 6.5ms, 계산은 약 2초.
- **패널 행 상한.** `HEADLINE_ROW_LIMIT`이 200인데 실측 최악이 198행이었다.
  600으로 올렸고, 이 값을 움직이는 것은 단어가 아니라 `collect_cap`이다.
